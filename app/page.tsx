import { EpubWorkspace } from "@/components/epub-workspace";

export default function Home() {
  return (
    <main className="flex min-h-0 flex-1 flex-col">
      <EpubWorkspace />
    </main>
  );
}